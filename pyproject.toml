[project]
name = "quote-strip-studio"
version = "0.4.0"
description = "Source-grounded YouTube quote-card pipeline with compact theme selection and high-clarity V4 typography."
requires-python = ">=3.9"
dependencies = [
    "Pillow>=10.0.0",
    "PyYAML>=6.0",
    "yt-dlp>=2025.1.1",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
qcard = "qcard.cli:main"
qcard-quality = "qcard.editorial_quality_v3.cli:main"
qcard-promote = "qcard.promote_selection:main"
qcard-render-v4 = "qcard.render_v4_cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["qcard*"]
